{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm run build --prefix frontend & npm link --prefix backend",
    "start": "cd backend && npm start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "npm i --prefix frontend & npm i --prefix backend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "author": "",
  "license": "ISC"
}
